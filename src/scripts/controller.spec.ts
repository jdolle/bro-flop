// import { default as expect } from 'unexpected'
// import td from 'testdouble'
//
// td.replace('keyboardjs')

describe('PlayerController', () => {
  describe('#init', () => {
    it('binds a, s, d, f, ⇦, ⇨, ⇧, and ⇩')
  })
})
